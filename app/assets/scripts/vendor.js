import '../../temp/scripts/modernizr';
import 'lazysizes';//lazyload for images (src = data-src + class="lazyload", when loaded changes to 'lazyloaded')
import 'picturefill';// for supports responsive imges in legasy browsers
